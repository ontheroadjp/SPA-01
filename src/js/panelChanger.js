
function initPanelChanger() {
	// var num = $('.carousel-inner').length;

	// for( n=1; n<num; n++) {
	// 	$('#my-carousel-'+n)
	// 		.carousel('pause')
	// 		.on('slid.bs.carousel', function () {
	// 			var a = $('.item.active').each(function(index) {
	// 				 // alert( $(this).children('section').attr('id') );
	// 			});
	// 		});
	// }
}